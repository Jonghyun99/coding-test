-- 코드를 작성해주세요
WITH RECURSIVE GEN_TREE AS (
    SELECT ID, 1 AS GEN
    FROM ECOLI_DATA
    WHERE PARENT_ID IS NULL
    UNION ALL
    SELECT A.ID, B.GEN + 1
    FROM ECOLI_DATA A
    JOIN GEN_TREE B ON A.PARENT_ID = B.ID
)

SELECT ID
FROM GEN_TREE
WHERE GEN=3
ORDER BY ID